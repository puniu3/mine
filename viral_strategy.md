# バイラル成長戦略：ワールド・カートリッジとディープリンク

本ドキュメントでは、ゲームの世界データを画像として保存・共有する機能（ワールド・カートリッジ）を核とした、バイラル成長のための実装戦略を定義する。

## 1. コア・コンセプト：ワールド・カートリッジ (World Cartridge)

「1ピクセル＝1タイル」の原則に基づき、PNG画像をゲームのセーブデータとして扱う。

### 1.1 仕様と制約
*   **データ構造**: ワールドサイズ（4096x256）に合わせたPNG画像を生成する。
*   **可逆性**:
    *   **Export**: 各ブロックID（`BLOCKS`）に対応する定義色（`BLOCK_PROPS`）でピクセルを描画。
    *   **Import**: 画像を読み込み、各ピクセルの色を最も近いブロック定義色に近似（Nearest Neighbor）してIDを復元する。これにより、SNS等による圧縮ノイズ（JPEG変換など）への耐性を持たせる。
*   **視認性**: 画像はそのまま「全体マップ」として機能する。

### 1.2 "カートリッジ"化（ブランディング）
単なる横長のマップ画像（16:1）はSNSでの見栄えが悪いため、以下の処理を加えるオプションを検討する。
*   **フレーム合成**: マップデータの周囲に、ゲームロゴ、作成者名、シード値、バイオーム統計などを記載した「枠（フレーム）」を付与する。
*   **メタデータ**: PNGのテキストチャンク（tEXt）に、プレイヤーのスポーン位置やインベントリ情報などの非可視データを埋め込む（※SNSで削除されるリスクがあるため、あくまで補助データとする）。

## 2. SNS連携とディープリンク戦略

画像ファイル自体の共有に加え、URLによる即時体験を提供する。

### 2.1 ハイブリッド共有モデル
ユーザーが「シェア」ボタンを押した際のフロー：
1.  **クラウド保存**: 現在のワールドデータを一時的なクラウドストレージ（S3/R2等）またはIPFSにアップロード。
2.  **ユニークURL生成**: `https://game-url.com/view?id=xyz...` 形式のリンクを発行。
3.  **動的OGP (Open Graph Protocol)**:
    *   リンク展開時に表示されるサムネイル画像として、ワールドの「見どころ（プレイヤー周辺など）」を切り出した魅力的な画像、または前述の「カートリッジ風」画像を動的に生成して返す。
    *   Twitter Card (Large Image) に対応させることで、タイムライン上での占有率を高める。

### 2.2 ディープリンクの挙動
*   URLを開くと、ブラウザ上でゲームが起動し、パラメータ指定されたワールドデータを自動的にロードする。
*   **"Play Now" 体験**: インストール不要のWebゲームである利点を活かし、1クリックで他人の作った世界に飛び込めるようにする。

## 3. バイラル・メカニクス（拡散の仕掛け）

### 3.1 「世界のリミックス」文化の醸成
*   **Fork & Edit**: 他人のワールドを読み込み、自分の建築や変更を加えた上で「Remix」として再エネクスポートできる機能。系譜（元のワールドID）を記録し、ツリー状の進化を可視化する。
*   **署名ブロック**: 特定の座標（例：岩盤層の隠しエリア）に、作成者の署名やメッセージをブロックパターンとして埋め込む。

### 3.2 隠し要素とトレジャーハント
*   **「宝の地図」として共有**: 通常の地形に見せかけて、特定の場所にレアアイテム（ジャックポットブロック等）を隠したワールドを配布し、「見つけられるか？」という挑戦状としてシェアさせる。
*   **ステガノグラフィ**: 一見するとただのドット絵（キャラクターやロゴ）だが、読み込むと実際に遊べるステージになっている「隠しレベル」を公式アカウントから配布し、話題を作る。

### 3.3 外部プラットフォーム活用
*   **Discord Bot**: ワールド画像をアップロードすると、自動的にプレビュー画像を生成したり、サーバー内のランキングに登録したりするBotを展開。
*   **Twitter Hashtag**: `#PixelWorldShare` などのタグを自動付与し、Botがそのタグを巡回して「今日のピックアップ・ワールド」を紹介する。

## 4. 技術的な実装ステップ（概要）

1.  **Image Encoder/Decoder**: `src/world/index.js` の `Uint8Array` と Canvas API 間の相互変換ロジック実装。色空間の量子化（Quantization）を含む。
2.  **UI実装**: タイトル画面に「Load from Image」ボタン、ポーズ画面に「Export as PNG」ボタンを追加。
3.  **Backend (Optional)**: シェア用URL発行のための軽量なAPIサーバー（Serverless Functions推奨）。
4.  **OGP Generator**: URLパラメータからワールドデータを取得し、OGP画像を返すエッジ関数。

この戦略により、ユーザーは「自分の作品を見せたい」という欲求と、「他人の世界で遊びたい」という欲求の両方を満たすことができ、画像の拡散と共にゲームの認知拡大が期待できる。
