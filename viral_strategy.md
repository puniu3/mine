# バイラル化戦略：世界共有機能の強化

本ドキュメントでは、既存の「世界をPNGとして書き出す」機能を活用し、SNS連携やシェアを通じてゲームの認知を拡大（バイラル化）させるための戦略を提案します。

## 1. コアコンセプト：「画像がそのままセーブデータになる（World Cartridge）」

単なるスクリーンショットではなく、PNG画像自体にセーブデータやワールド生成シードを埋め込む**ステガノグラフィ技術**を採用します。

*   **仕組み**: PNGのメタデータ（tEXtチャンク）や、画像の最下部/アルファチャンネルに見えない形でデータをエンコードします。
*   **UX**:
    *   ユーザーは画像を保存し、友人に送ったりSNSに投稿します。
    *   受け取った友人は、その画像をゲーム画面に**ドラッグ＆ドロップ**するだけで、同じ世界の同じ場所に降り立つことができます。
    *   **「この画像を持っているだけで続きから遊べる」**という体験は、レトロゲームのカセットのような収集欲と共有欲を刺激します。
*   **実装難易度**: 中（Canvas操作とバイナリ操作が必要）

## 2. シェア導線の強化（ディープリンクとWeb Share API）

画像共有時の摩擦（Friction）を減らし、URL経由での流入を最大化します。

### A. URLパラメータによるディープリンク
画像データがSNSの圧縮で削除されてしまう場合に備え、共有テキストには必ず**再現用URL**を含めます。

*   **URL設計**: `https://game-url.com/?seed=12345&x=1024&y=64`
*   **挙動**: このリンクを開くと、指定されたシード値でワールドが生成され、指定された座標にカメラ/プレイヤーが配置された状態でスタートします。

### B. Web Share API (モバイル向け)
モバイル端末では、OSネイティブのシェアシート（共有メニュー）を呼び出します。

*   `navigator.share()` を使用し、画像ファイルとテキスト（URL含む）を一度にTwitterやLINE、Discordに渡せるようにします。
*   「画像を保存→アプリ切り替え→添付」という手順を「シェアボタン→アプリ選択」の1ステップに短縮します。

## 3. バイラル・ループの設計（拡散の動機づけ）

ユーザーがシェアしたくなる「文脈」をシステム側で用意します。

### A. 「探検ログ」としての透かし（Watermark）
書き出されるPNG画像に、以下の情報を自動的に合成します。

*   **ゲームロゴ**: 認知拡大のため必須。
*   **QRコード**: スマホで画像を見た人がすぐにアクセスできるように。
*   **座標/シード値**: テキストで記載し、手入力でも再現可能にする。
*   **日時**: 思い出としての価値を高める。

### B. SNSシェア文言の工夫 (Twitter/X Intents)
シェアボタンを押した際のデフォルト文言を工夫します。

*   **挑戦状型**: 「このマップの地下深くでダイヤモンドを見つけた！君は辿り着けるか？ #VoxelGame」
*   **絶景共有型**: 「座標(X,Y)のオーロラが最高だった。 #VoxelGame」
*   **ハッシュタグ活用**: ゲーム名タグに加え、`#VoxelGameSeed` のような専用タグを作り、ユーザー同士のシード交換文化を作ります。

## 4. 技術的制約と対策

### クライアントサイドのみでの限界
本ゲームはサーバーレス（静的サイト）であると想定されるため、動的なOGP（Twitterカード等のリンクプレビュー画像）の生成が困難です。

*   **対策**:
    *   **フェーズ1（現状）**: OGPは固定の公式画像にする。ユーザーが投稿する「添付画像」で見せる戦略をとる。
    *   **フェーズ2（発展）**: Cloudinaryなどの外部サービスや、AWS Lambdaなどの軽量なFaaSを利用し、URLパラメータから動的にOGP画像を生成するプロキシを噛ませる（要バックエンド実装）。

## 5. 推奨される実装ロードマップ

1.  **Lv.1 (基本)**:
    *   書き出し画像にゲームロゴとURLを合成する（透かし）。
    *   Twitterシェアボタンを設置し、現在のシード値と座標を含むURLをツイート文言に埋め込む。
2.  **Lv.2 (体験向上)**:
    *   URLパラメータ(`?seed=...`)を読み取り、ゲーム開始時にその地点へテレポートする機能を実装。
    *   Web Share APIを実装し、画像ファイルを直接SNSへ投げられるようにする。
3.  **Lv.3 (独自性)**:
    *   PNGステガノグラフィを実装し、ドラッグ＆ドロップでのワールド読み込みに対応する。

まずは **Lv.1 と Lv.2** の実装により、テキストリンクと魅力的な画像のセットで拡散を狙うのが最もコスト対効果が高い戦略となります。
