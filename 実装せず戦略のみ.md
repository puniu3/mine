# ワールド・カートリッジ戦略 (World Cartridge Strategy)

## 概要
サーバを使用せず、PNG画像そのものを「セーブデータ（カートリッジ）」として扱い、SNSやDiscordを通じてワールドデータを共有する仕組みを構築する。
画像は1ピクセルが1タイルに対応する「世界地図」そのものであり、メタデータやQRコードなどの特殊な加工は一切施さない。

## 技術仕様

### 1. データ形式
*   **フォーマット:** PNG (Portable Network Graphics)
*   **解像度:** 4096 x 256 ピクセル (世界のタイル数 `WORLD_WIDTH` x `WORLD_HEIGHT` と完全に一致)
*   **色深度:** 32-bit RGBA (TrueColor + Alpha)

### 2. エクスポート (エンコード) 戦略
現在のワールドデータ (`Uint8Array`) を走査し、Canvas APIを用いて画像化する。

*   **マッピングロジック:**
    *   `world.blocks` 配列のインデックス `i` を `(x, y)` 座標に変換。
    *   **ブロックID = 0 (AIR) の場合:**
        *   完全な透明ピクセル `RGBA(0, 0, 0, 0)` として描画する。
        *   これにより、背景色に依存せず、SNS上で「切り抜き画像」のように表示される可能性があるが、データとしては最も純粋である。
    *   **ブロックID > 0 の場合:**
        *   `src/constants.js` の `BLOCK_PROPS` で定義された `color` (Hex) を取得し、RGB値に変換して描画する。
        *   例: DIRT -> `#5d4037` -> `RGB(93, 64, 55)`

### 3. インポート (デコード) 戦略
ユーザーが画像をゲームにドラッグ＆ドロップ、またはファイル選択した際に実行される。
ブラウザのCanvas API (`drawImage` -> `getImageData`) を使用してピクセルデータを読み取る。

*   **復元ロジック (Nearest Color Matching):**
    *   SNS等での圧縮アーティファクト（JPEG変換や再エンコードによる色のわずかな変化）を許容するため、**完全一致ではなく「最も近い色」**を探すアルゴリズムを採用する。
    *   **判定プロセス:**
        1.  ピクセルのアルファ値を確認。
            *   `Alpha < 128` (半透明以下) -> `BLOCKS.AIR` (0) と判定。
        2.  `Alpha >= 128` の場合、ピクセルの `(R, G, B)` と、`BLOCK_PROPS` に定義された全ブロックの色のユークリッド距離を計算する。
            *   `Distance = sqrt((R1-R2)^2 + (G1-G2)^2 + (B1-B2)^2)`
        3.  距離が最も小さいブロックIDを採用して `world.blocks` に書き込む。

### 4. ユーザー体験 (UX) フロー

#### A. エクスポート (共有)
1.  ポーズメニューに「ワールドを書き出す (Export World)」ボタンを追加。
2.  ボタンを押すと `canvas.toBlob()` が実行され、`world.png` が生成される。
3.  ブラウザのダウンロード機能により、ローカルに保存される。
4.  ユーザーへのメッセージ: 「この画像をDiscordやSNSに投稿してシェアしよう！友達はこの画像を読み込むだけで遊べるよ」

#### B. インポート (読み込み)
1.  **ドラッグ＆ドロップ:**
    *   タイトル画面またはポーズ画面の全体にドラッグ＆ドロップ領域を設定。
    *   画像ファイル (`image/*`) がドロップされると、即座に読み込み処理を開始する。
2.  **ファイル選択:**
    *   「ワールドを読み込む」ボタンを用意し、クリックでファイルピッカーを開く。
3.  読み込み完了後、即座にそのワールドへ遷移する。

## 技術的制約と対策

### サーバレス構造
*   データは全てユーザーのローカル環境と、ユーザーが利用する外部プラットフォーム（Discord, Twitter等）を経由するため、自社サーバでのストレージ管理は不要。

### SNSによる画像圧縮問題
*   **Twitter (X):** PNGをJPGやWebPに変換する場合がある。
*   **Discord:** 一般的にオリジナルファイルを保持する傾向がある。
*   **対策:**
    *   前述の「近似色マッチング」により、不可逆圧縮による色の変化（モスキートノイズ等）があっても、元の色が `DIRT` に近ければ `DIRT` として復元できる。
    *   解像度自体がリサイズされた場合（4096px -> 2048px等）は読み込みエラーとするか、最近傍法 (Nearest Neighbor) で拡大して読み込む警告を出す。

### クロスオリジン (CORS) 問題
*   Web上の画像URLを直接読み込むと `Tainted Canvas` となり、ピクセルデータにアクセスできなくなる。
*   **解決策:** ユーザーに一度画像を「保存（ダウンロード）」させ、ローカルファイルとしてドラッグ＆ドロップさせるフローに限定することで、CORS制約を回避する。

## まとめ
この戦略により、追加のインフラコストをかけずに、画像という親しみやすいメディアを使ってワールドデータを共有・拡散させる導線（バイラル・ループ）を構築できる。画像は単なるセーブデータではなく、その見た目自体がワールドの「プレビュー」を兼ねている点が最大の特徴である。
